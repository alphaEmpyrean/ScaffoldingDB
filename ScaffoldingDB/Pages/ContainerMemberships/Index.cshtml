@page
@model ScaffoldingDB.Pages.ContainerMemberships.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContainerMembership[0].Weight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContainerMembership[0].LocalStake)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContainerMembership[0].Container)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContainerMembership[0].Staker)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ContainerMembership) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Weight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocalStake)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Container.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Staker.Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
